package com.ide.api.repository;

import com.ide.api.entities.ContenuParCategories;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ContenuParCategorieRepository extends JpaRepository<ContenuParCategories, Integer> {
}
